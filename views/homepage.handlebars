<ol>
    {{#each posts}}
    <li>
        <article class="post">
            <div class="title">
                <div>
                    <p>
                        {{title}}
                    </p>
                </div>
                <div class="meta">
                    posted by {{user.username}} on {{format_date created_at}}
                    |
                    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
                </div>
            </div>
        </article>
    </li>
    {{/each}}
</ol>